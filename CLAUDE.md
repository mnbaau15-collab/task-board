# CLAUDE.md

このファイルは、本プロジェクト（task-board）で Claude Code が作業する際のガイドラインです。

## プロジェクト概要

タスクボード（task-board）アプリケーション。
技術スタックやディレクトリ構成が決まったら、このセクションに追記すること。

## 開発ルール

- 既存コードのスタイル・命名規則・コメント量に合わせる
- 変更は依頼された範囲に絞り、無関係なリファクタリングを混ぜない
- 変更後は可能な範囲でテスト・ビルド・リントを実行して確認する

## Git運用ルール

- **コードを変更するたびに、コミットして GitHub にプッシュする**
- 1つの意味のある変更ごとにコミットする（複数の変更をまとめない）
- コミットメッセージは変更内容が分かる簡潔な文にする
- プッシュ前に `git status` / `git diff` で変更内容を確認する
- 機密情報（APIキー、`.env` など）は絶対にコミットしない
- `main` ブランチへ直接作業する場合を除き、作業ブランチを切ってからプッシュする
- force push（`--force`）は行わない

## コミュニケーション

- ユーザーへの返答は日本語で行う
